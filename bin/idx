#!/bin/bash

rm -f cscope.* tags
if [ -e "./idx.files" ];
then
    cscope -bkq -i./idx.files
    ctags -R -L ./idx.files
else
    cscope -bkRq
    ctags -R
fi

### for cctree built from ccglue.sourceforge.net
### ccglue -S cscope.out -o cctree.out
